node_modules/
dist/
dist-demo/
dist-ssr/
*.local
.DS_Store
.vite/
*.tsbuildinfo

# Demonstração de página única (npm run build-demo)
demo/

# Saída do teste de fumaça
saida-fumaca/

# Binários e conteúdo gerado do servidor portátil.
# O diretório web/ existe apenas para o `go:embed`; o conteúdo real é copiado
# de dist/ pelo scripts/build-portatil.sh.
portatil/
cmd/servidor/web/*
!cmd/servidor/web/.gitkeep
cmd/servidor/servidor
cmd/servidor/servidor.exe

# Saída da bateria de verificação
saida-testes/

# Fila de liberação gravada pelo servidor em execução
dados/
cmd/servidor/dados/
